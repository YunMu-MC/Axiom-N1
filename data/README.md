# Data Directory

Generated corpora, distillation outputs, train/valid shards, and evaluation dumps are intentionally excluded from Git.

Place local datasets here when running experiments, or pass an explicit output directory on the command line. Do not commit raw dialogue corpora, API generations, checkpoints, or private evaluation traces.
